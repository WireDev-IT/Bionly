<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Bionly.ViewModels"
             x:Class="Bionly.Views.MeasurementsPage" Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:MeasurementsViewModel />
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Demo" Command="{Binding RefreshFiles}"/>
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <ListView ItemsSource="{Binding Values}" Margin="10">
            <ListView.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Text="Zeit"/>
                    <Label Grid.Column="1" Text="Temperatur"/>
                    <Label Grid.Column="2" Text="Luftfeuchtigkeit"/>
                    <Label Grid.Column="3" Text="Luftdruck"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Text="{Binding TimeStr}"/>
                            <Label Grid.Column="1" Text="{Binding TemperatureStr}"/>
                            <Label Grid.Column="2" Text="{Binding HumidityStr}"/>
                            <Label Grid.Column="3" Text="{Binding PressureStr}"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>